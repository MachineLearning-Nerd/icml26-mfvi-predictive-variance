# Branch audit

## Final branch map

| Branch | Role | State |
| --- | --- | --- |
| `main` | Canonical paper source, independent verifier, retained full-scale outputs, and publication gate | Active and published |

The original repository exposed only `main`. No alternate branch carried a different implementation, dataset, or result, so there was no branch-specific behavior to preserve or rename. Future experiments should use short-lived branches and merge reviewed evidence into `main`.
