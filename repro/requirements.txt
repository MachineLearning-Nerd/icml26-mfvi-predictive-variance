# Minimal dependencies imported by the official nine-UCI trace path and its
# independent verifier. Torch itself is installed from the PyTorch cu121 index
# in the documented bootstrap command.
pandas==2.3.3
matplotlib==3.11.1
numpy==2.5.2
scipy==1.16.1
pytest==8.4.2
